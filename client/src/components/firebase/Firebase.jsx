import React from 'react'
import {Button} from "@chakra-ui/react"

const Firebase = () => {
    return (
        <div>
            <Button>Login</Button>
        </div>
    )
}

export default Firebase
